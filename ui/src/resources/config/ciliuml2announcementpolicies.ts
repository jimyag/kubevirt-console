import type { ResourceConfig } from "@/components/resource-management";
import * as shared from "./shared";

export const ciliumL2AnnouncementPoliciesConfig: ResourceConfig = shared.ciliumResourceConfig({ version: "v2alpha1", plural: "ciliuml2announcementpolicies", path: "/networks/cilium/l2-announcement-policies", title: "Cilium L2 Announcement Policies", subtitle: "Manage Cilium L2 announcement policies", kind: "CiliumL2AnnouncementPolicy", createFields: [{ name: "interfaces", label: "Interfaces", section: "L2", defaultValue: "", placeholder: "comma-separated; empty means all" }, { name: "externalIPs", label: "External IPs", section: "L2", type: "checkbox", defaultValue: true }, { name: "loadBalancerIPs", label: "LoadBalancer IPs", section: "L2", type: "checkbox", defaultValue: true }], buildSpec: (values) => ({ ...(shared.csvList(values.interfaces).length ? { interfaces: shared.csvList(values.interfaces) } : {}), externalIPs: values.externalIPs === true, loadBalancerIPs: values.loadBalancerIPs === true }), statusPath: ["status", "conditions", "0", "type"] });
