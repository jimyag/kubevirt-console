apiVersion: v1
kind: Namespace
metadata:
  name: kubevirt-dashboard
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubevirt-dashboard
  namespace: kubevirt-dashboard
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kubevirt-dashboard
rules:
  - apiGroups:
      - "subresources.kubevirt.io"
    resources:
      - virtualmachineinstances/console
    verbs:
      - get
  - apiGroups:
      - "kubevirt.io"
    resources:
      - virtualmachineinstances
    verbs:
      - list
      - get
      - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubevirt-dashboard
subjects:
  - kind: ServiceAccount
    name: kubevirt-dashboard
    namespace: kubevirt-dashboard
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kubevirt-dashboard
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubevirt-dashboard
  namespace: kubevirt-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubevirt-dashboard
  template:
    metadata:
      labels:
        app: kubevirt-dashboard
    spec:
      serviceAccountName: kubevirt-dashboard
      containers:
        - name: kubevirt-dashboard
          image: ghcr.io/jimyag/kubevirt-dashboard:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          args:
            - web
            - --listen
            - "0.0.0.0:8080"
          ports:
            - containerPort: 8080
              name: web
          readinessProbe:
            httpGet:
              path: /
              port: web
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: web
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: kubevirt-dashboard
  namespace: kubevirt-dashboard
spec:
  selector:
    app: kubevirt-dashboard
  ports:
    - name: console
      port: 80
      targetPort: web
      protocol: TCP
  type: ClusterIP
